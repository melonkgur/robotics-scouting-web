import { createTheme } from "@material-ui/core";


const theme = createTheme({
    palette: {
        primary: {
            main: '#0f172a'
        },
        secondary: {
            main: '#E879F9'
        }
    },
    typography: {
        fontFamily: {

        }
    }
})

export default theme;